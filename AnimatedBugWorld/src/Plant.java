import javafx.scene.paint.Color;
import javafx.stage.Stage;

public class Plant extends Bug {//may change this to just extend circle but for now plant
//	circles still use many of the same fields and methods as bug circles

	public Plant(Stage primaryStage, double x, double y, double rad, Color color) {
		super(primaryStage, x, y, rad, color);
		
	}
	
	

}
